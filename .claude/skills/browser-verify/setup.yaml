# Skill setup metadata — dependencies, MCP servers, and permissions
permissions:
  filesystem: read-write  # screenshots, PDFs, temp files
  network: true           # CDP protocol + browser network requests
  shell: true             # runs chrome-launcher.sh and cdp.mjs via bash/node
