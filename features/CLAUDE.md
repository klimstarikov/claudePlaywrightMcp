# Feature Files — Rules

- Pure Gherkin (`.feature`) documentation only — not executed by a Cucumber runner.
- Each feature file must have a corresponding spec in `tests/`.
- Keep scenarios focused: one scenario per user behaviour.
- File name pattern: `<feature-name>.feature`.
